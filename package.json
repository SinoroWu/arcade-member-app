{
  "name": "arcade-member-app-root",
  "version": "1.0.0",
  "description": "Arcade Membership App - Full Ecosystem",
  "scripts": {
    "dev:backend": "npm --prefix backend run start",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev": "npm --prefix frontend run dev",
    "db:push": "npm --prefix backend run db:push",
    "db:seed": "npm --prefix backend run db:seed",
    "build": "npm --prefix backend install",
    "build:full": "npm --prefix frontend install && npm --prefix frontend run build && npm --prefix backend install && npm --prefix backend run build",
    "start": "node backend/dist/server.js"
  }
}
